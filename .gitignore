__pycache__/
*.pyc
.env
.DS_Store

# Foundry build artifacts and generated PoC test files (poc_verifier.py
# writes gate 3's Foundry test here transiently, then deletes it — this
# is a backstop in case a run is killed before cleanup).
out/
cache/
verify/*.t.sol
